# Контрольная точка 12 мая 2026 года

Это историческая запись проверки Linux-пакета на commit
`b740d6603787da701687a4fae421f7c68c94f9a8`.

## Что было проверено

- сборка `.deb` размером около 52 МБ;
- установка `telegram-username-collector`;
- наличие `/usr/bin/telegram-username-collector`, `/usr/bin/sitectl` и
  `/opt/telegram-username-collector`;
- создание XDG-каталогов пользователя;
- запуск GTK-окна через desktop-файл;
- отсутствие runtime-файлов внутри `/opt`.

Команда диагностики завершилась с предупреждением `hub_reachable=0`, потому
что хаб не был запущен. Ошибка `127` в конце старого лога относилась к
отсутствующему `rg`, а не к продукту.

## Итог

Старый контур установки признан работоспособным с предупреждением. В тот же
день выяснилось, что GitHub `main` уже перешёл на
`45c25e4fc5641a807a809f173ac0cfeaed798934`, поэтому потребовалась отдельная
повторная проверка нового базового состояния.

Точный старый журнал доступен в истории Git. Этот файл не описывает текущую
версию продукта.
