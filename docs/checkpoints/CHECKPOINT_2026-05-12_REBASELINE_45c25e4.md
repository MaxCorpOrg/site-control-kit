# Повторная базовая проверка 12 мая 2026 года

Исторический прогон проверял `origin/main` на
`45c25e4fc5641a807a809f173ac0cfeaed798934` в отдельном worktree и на хосте
`maxcorp-server`.

## Найденная регрессия

Установленный продукт пытался создать
`/opt/telegram-username-collector/app/.site-control-kit` и завершался с
`PermissionError`. Причина: локальный конфиг разрешался раньше пользовательских
XDG-путей.

## Исправление

- `webcontrol/settings.py` получил installed-mode путь локального конфига;
- wrappers стали передавать `SITECTL_LOCAL_CONFIG_PATH`;
- добавлены регрессионные тесты настроек и диагностики.

## Проверка

- профильные тесты прошли;
- полный набор: 305 тестов;
- `.deb` пересобран и переустановлен;
- диагностика показала `overall_status=ok` и `hub_reachable=1`;
- GTK-окно открылось;
- XDG-каталоги создавались вне `/opt`;
- утечки runtime-файлов в каталог приложения не обнаружены.

Сервер использовал Xvfb, fluxbox и x11vnc, поэтому обычное GNOME-меню было
проверено отдельной контрольной точкой. Полные старые команды и пути сохранены
в истории Git.
